# A workflow that cannot run is refused before anything is started, and says which check
# refused it. Nothing here executes.
# expect: invalid
# refuses: needs-exist
name: Refused when invalid

on: push

jobs:
  build:
    needs: nowhere
    runs-on: ubuntu-latest
    steps:
      - run: echo "this should never run"
