[package]
name = "gh-actions-encoding"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The encodings GitHub wraps a job's JSON in, read and written by serde"

[dependencies]
serde = { workspace = true }
serde_json = "1.0"
